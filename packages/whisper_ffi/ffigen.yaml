# Run with `dart run ffigen --config ffigen.yaml`.
name: WhisperFfiBindings
description: |
  Bindings for `src/whisper_ffi.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/whisper_ffi_bindings_generated.dart'
headers:
  entry-points:
    - 'src/whisper_ffi.h'
  include-directives:
    - 'src/whisper_ffi.h'
ffi-native:
comments:
  style: any
  length: full
